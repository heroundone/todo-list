

function addToDoEventListener(submitButton) {
    // get submit button
    submitButton = document.getElementById('newToDo');
    submitButton.addEventListener('click', () => {

    })
}

function todoFormEventListener() {

}

function checkTextContent(element) {
    if (element.checkTextContent = '') {
        return false;
    }
    else {
        return true;
    }

}