when clicking on 'add project' and 'add todo' check if the respective forms already exists
if (form exists)
    then return
else
    continue


based on priority, projects have different border colors

checklist for todo items. button or div that when clicked is filled with an x for text content, or checkmark image